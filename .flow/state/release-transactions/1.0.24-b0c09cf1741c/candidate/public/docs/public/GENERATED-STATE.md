# Generated state

Generated state 是基于明确源码和数据输入生成的可校验数据状态。

其 manifest 记录：

- 构建标识和基线提交；
- 输出文件的 SHA-256 和大小；
- 数据验证结果；
- 可选 npm 数据包审计引用。

公开 `online` 分支可保存最新数据投影、来源诊断证据、验证报告和发布记录，但不承载项目源码、内部维护配置或原始网页缓存。

不可变归档会拒绝绝对路径、路径穿越、符号链接、重复成员和异常膨胀，并通过 receipt 绑定归档哈希、文件数量和 manifest。
